{
  "description": "Safety hooks for domain-agent-kit. (1) PreToolUse: surfaces a native Claude Code confirmation prompt before irreversible Dynadot operations. Non-destructive ops pass through unchanged; parse failures fail closed. (2) SessionStart: fast credential presence check with a loud warning when neither a project settings file nor a shell DYNADOT_API_KEY env var is configured.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__domain-mcp__domain|mcp__domain-mcp__transfer|mcp__domain-mcp__aftermarket|mcp__domain-mcp__contact|mcp__domain-mcp__folder",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/confirm-destructive.sh",
            "timeout": 15
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/session-preflight.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
