{
  "description": "Optional Claude Code confirmation adapter for irreversible domain-mcp operations. Canonical safety policy lives in skills/domain-management/references/safety.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__domain-mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/confirm-destructive.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
