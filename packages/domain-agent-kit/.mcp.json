{
  "mcpServers": {
    "domain-mcp": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/launch-domain-mcp.sh"],
      "env": {
        "DOMAIN_MCP_VERSION": "^3.0.0"
      }
    }
  }
}
